package entity.Clases;

public class Tutoriales {
    public static void main(String[] args) {

        /*
        //TIPOS DE DATOS PRIMITIVOS
        -int = numero entero
        -double = numero con coma
        -char = un caracter ej: a
        -boolean = verdadero y falso

        //int (redondea para abajo)
        //Math.round (redondea para arriba)
        //Math.random ()



        //CONVERTIR DATO A OTRO
        /CASTEO
        (double a int)
        double n1 = 2.3;
        int n1E = (int)n1; --> "2.3"

        (int a texto)
        int num= 123
        Integer,toString(num); --> convierte el int a String

        /PARSEO (convertir una cadena de texto a int)
        String numTexto = "123"

        Integer.valueOF(numTexto); --> Devuelve un Int clase
                    o
        Integer.poarseInt(numTexto); --> Devuelve un Int primitivo






        //CLASES (Todas las clases empiezan con MAYUSCULAS)
        -String = cadena de texto
        -Boolean =
        -Float = permite hasta 7 decimales
        -Double = permite hasta 16 decimales
        -LocalDate = fechas (año, mes, dia)
            LocalDate.now() --> Muestra la fecha actual
            LocalDate.of()--> Crear Fecha
            LocalDate.getYear
        -Integer --> es como el dato primitivo int


        //METODOS
        String saludo = "Hola"
        String saludo2 = "HOLA"
        String saludo3 = "Hola,nene"

        saludo.length() -git -> cuenta los caracteres
        saludo.equals(saludo2)  --> compara los dos textos --> Retorna un Boolean
        saludo.toUpperCase() --> convierte el texto en MAYUSCULAS
        saludo.charAT(2) --> muestra el caracter en la posicion 2: 'o'
        saludo.CompareTO()--> ordena los caracteres es Unicode(codigo de valor numerico)(para ordenar numeros es muy bueno)
        saludo.getClass --> muestra que tipo es (int,double, etc...)
        saludo3.split(",") --> separa el String dependiendo de lo que se pone en el parentesis
        saludo,substring(1) --> "ola" --> muestra los caracteres a partir del numero elegido


        //FUNCIONES (se escriben por fuera del metodo public static void main)

        public static void "nombredeclase"("dato a recibir2" "nombredeclase2" ){}

        //RECORRER ARRAY
        String[] comidas = {"pizza","papa","hamburguesa","cebolla"};
        for(String elementos:comidas){
            System.out.println(elementos);

        Arrays.Sort()-->Orden alfabetico

        //INGRESAR VALOR POR CONSOLA
        Scanner scanner = new


        //CREAR OBJETO


        this --> hace referencia a el objeto
        get --> consultar
        set --> modificar






        //GIT HUB
        SUBIR CAMBIOS
        git status --> para ver que archivos, en rojo es que no estan guardados

        git branch --> ver en que rama estoy
        git checkout -->

        git add .
        git commit -m "Mensaje"
        git push origin clase4 --> clase4 es un ejemplo

         */


    }

}
